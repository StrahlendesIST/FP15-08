def explode(x):
    if x == int(x) and x > 0:
        t = ()
        while x > 0:
            t = (x % 10,) + t
            x = x // 10
        return t
    else:
        raise ValueError('explode: argumento nao inteiro')
